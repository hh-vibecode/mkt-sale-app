name: Sync KiotViet (đơn đặt hàng + hoá đơn)

on:
  schedule:
    # 30 phút/lần -- chạy INCREMENTAL (5 trang mới nhất/shop), nhẹ, đủ bắt đơn mới.
    - cron: '*/30 * * * *'
    # 7h05 sáng VN (00:05 UTC) -- lượt BACKFILL quét lại toàn bộ từ 1/6, bắt cả sửa đổi trên đơn cũ.
    # Lệch 5 phút khỏi giờ tròn vì GitHub Actions hay dồn/rớt cron đúng phút :00.
    - cron: '5 0 * * *'
  workflow_dispatch:
    inputs:
      backfill:
        description: 'Quét lại toàn bộ hoá đơn từ 1/6/2026'
        type: boolean
        default: false

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: {node-version: '20'}
      - name: Sync hoá đơn KiotViet -> Supabase
        env:
          KIOT_CLIENT_ID: ${{ secrets.KIOT_CLIENT_ID }}
          KIOT_CLIENT_SECRET: ${{ secrets.KIOT_CLIENT_SECRET }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          BACKFILL: ${{ (github.event.schedule == '5 0 * * *' || inputs.backfill) && '1' || '0' }}
        run: node scripts/sync-kiot-invoices.js
      # Đơn đặt hàng = nguồn doanh số + ngày chốt của báo cáo Sale (hoá đơn phía trên chỉ để đối soát).
      # Luôn quét lại toàn bộ từ 1/6 (~9 request) để bắt cả đơn cũ bị huỷ / đổi tiền.
      - name: Sync đơn đặt hàng KiotViet -> Supabase
        if: always()
        env:
          KIOT_CLIENT_ID: ${{ secrets.KIOT_CLIENT_ID }}
          KIOT_CLIENT_SECRET: ${{ secrets.KIOT_CLIENT_SECRET }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: node scripts/sync-kiot-orders.js
